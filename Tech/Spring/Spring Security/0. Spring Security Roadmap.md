The index for these notes. Seventeen notes that build from "what is a filter" up to OAuth2, token security and the browser-facing defences — every concept introduced only after the problem it solves.

> [!info] How to use this
> Read **1 → 17 in order** the first time; each note assumes the one before it. Notes **1–10** are the core you must know cold; **11–17** are the deep dives (resource server, SSO, OIDC, ABAC, CORS/CSRF/headers) where senior interviews actually spend their time. After that, use the priority table to revise, and the **⚡ Quick Recall** box at the bottom of every note as the night-before pass. Every note opens with a **🧭 Summary** callout that is the whole note in bullets.

---

## 🗺️ The full map

```text
Layer 0   The architecture everything plugs into
          1. Filters and SecurityFilterChain

Layer 1   Authentication — "who are you?"
          2. Authentication Pipeline
          3. Password Encoding and BCrypt
          4. SecurityContext, Authentication and Principal
          5. Form Login and HTTP Basic

Layer 2   Authorization — "what may you do?"
          6. Authorization

Layer 3   Stateless and distributed
          7. JWT Authentication
          8. OAuth2 and OpenID Connect
          9. Refresh Tokens and Token Security

Layer 4   The browser-facing details
          10. CSRF, CORS and Session Management

Layer 5   OAuth2 in practice — the two sides of the protocol
          11. OAuth2 Resource Server        (you RECEIVE a token)
          12. OAuth2 Login and SSO          (you GO AND GET one)
          13. OpenID Connect Deep Dive      (who the user is)

Layer 6   Authorization models
          14. RBAC and ABAC

Layer 7   Browser security, in depth
          15. CORS Deep Dive
          16. CSRF Deep Dive
          17. HTTP Security Headers
```

---

## 📚 The notes

| # | Note | What it answers | Priority |
|---|---|---|---|
| 1 | [[1. Filters and SecurityFilterChain]] | How does a request reach my controller? Why is everything a filter? | ⭐⭐⭐⭐⭐ |
| 2 | [[2. Authentication Pipeline]] | `AuthenticationManager` → `Provider` → `UserDetailsService` → `PasswordEncoder` | ⭐⭐⭐⭐⭐ |
| 3 | [[3. Password Encoding and BCrypt]] | Salt, cost factor, hash anatomy, migrating hashes | ⭐⭐⭐⭐ |
| 4 | [[4. SecurityContext, Authentication and Principal]] | Where the current user lives, and what's inside `Authentication` | ⭐⭐⭐⭐⭐ |
| 5 | [[5. Form Login and HTTP Basic]] | The two classic entry filters; stateful vs stateless | ⭐⭐⭐ |
| 6 | [[6. Authorization]] | URL vs method security, `hasRole` vs `hasAuthority`, 401 vs 403, RBAC/ABAC | ⭐⭐⭐⭐⭐ |
| 7 | [[7. JWT Authentication]] | Stateless auth, the custom filter, and the revocation problem | ⭐⭐⭐⭐⭐ |
| 8 | [[8. OAuth2 and OpenID Connect]] | Four actors, Authorization Code + PKCE, scopes, resource server | ⭐⭐⭐⭐⭐ |
| 9 | [[9. Refresh Tokens and Token Security]] | Who controls access, rotation, revocation, storage | ⭐⭐⭐⭐ |
| 10 | [[10. CSRF, CORS and Session Management]] | The two most-confused acronyms + session hardening | ⭐⭐⭐⭐ |
| 11 | [[11. OAuth2 Resource Server]] | `BearerTokenAuthenticationFilter`, JWKS, `issuer-uri`, scope/role mapping | ⭐⭐⭐⭐⭐ |
| 12 | [[12. OAuth2 Login and SSO]] | The full Google-login flow, `OAuth2User`, external identity + internal roles | ⭐⭐⭐⭐ |
| 13 | [[13. OpenID Connect Deep Dive]] | ID token vs access token, `nonce`, discovery, `OidcUser` | ⭐⭐⭐⭐⭐ |
| 14 | [[14. RBAC and ABAC]] | Ownership rules, SpEL, `PermissionEvaluator`, role explosion | ⭐⭐⭐⭐⭐ |
| 15 | [[15. CORS Deep Dive]] | SOP, simple vs preflight, why Postman works, the Spring gotcha | ⭐⭐⭐⭐ |
| 16 | [[16. CSRF Deep Dive]] | The attack, the token asymmetry, JWT-in-cookie vs header | ⭐⭐⭐⭐⭐ |
| 17 | [[17. HTTP Security Headers]] | Clickjacking, CSP, HSTS and the first-request problem | ⭐⭐⭐ |

---

## 🧠 The one diagram to hold in your head

Everything in these notes hangs off this:

```text
Request
   |
   v
DelegatingFilterProxy  ->  FilterChainProxy  ->  SecurityFilterChain
   |
   v
Authentication Filter                     [note 1, 5, 7, 8, 11, 12]
   |   form login / HTTP basic / JWT / bearer token / OAuth2 redirect
   |
   v
AuthenticationManager                     [note 2]
   |   -> AuthenticationProvider
   |        -> UserDetailsService  (load the user)
   |        -> PasswordEncoder     (verify the password)  [note 3]
   |
   v
Authentication  ->  SecurityContextHolder [note 4]
   |   Principal + Credentials + Authorities + authenticated
   |
   v
AuthorizationFilter                       [note 6]
   |   hasRole / hasAuthority / permitAll
   |
   v
DispatcherServlet  ->  Controller
   |
   v
@PreAuthorize on the service              [note 6, 14]
   |   RBAC (roles) + ABAC (attributes)
```

**Authentication filters *write* to `SecurityContextHolder`; authorization *reads* from it.** Every topic — form login, JWT, OAuth2, method security — is a variation on which filter does the writing.

---

## ⚡ The answers worth memorising

| Question | Answer |
|---|---|
| What is a `SecurityFilterChain`? | An ordered list of security filters + a matcher; only the first matching chain runs |
| Who verifies the password? | `AuthenticationProvider` + `PasswordEncoder` — **not** `UserDetailsService` |
| Why can't you compare BCrypt hashes with `equals`? | New random salt every `encode()` → use `matches()` |
| Where is the current user stored? | `SecurityContextHolder` → `SecurityContext` → `Authentication`, in a **`ThreadLocal`** |
| `hasRole("ADMIN")` vs `hasAuthority("ROLE_ADMIN")` | Identical — `hasRole` just prepends `ROLE_` |
| 401 vs 403 | 401 = not authenticated · 403 = authenticated but not permitted |
| OAuth2 vs JWT | Protocol vs token format — OAuth2 may issue opaque tokens instead |
| Why an authorization code, not a token, in the redirect? | The redirect goes through the browser (URL, history, logs); the code is single-use and useless alone |
| How do you log out with JWT? | You can't, statelessly — short expiry + `jti` denylist + revoke the refresh token |
| When can you disable CSRF? | Only when credentials aren't attached automatically, i.e. header-based `Bearer` tokens |
| Resource Server vs OAuth2 Login | One **receives** a token and validates it; the other **goes and gets** one by redirecting to an IdP |
| Access token vs ID token | Boarding pass (`aud` = the API) vs passport (`aud` = your `client_id`). Never swap them |
| What does `issuer-uri` buy you? | OIDC discovery — Spring learns the JWKS and token endpoints, and validates the `iss` claim |
| Why does RBAC fail on "view your own order"? | Every customer is `ROLE_CUSTOMER`; roles can't express **ownership**. That's ABAC's job |
| Does CORS block the request? | No — it blocks JavaScript **reading the response**. The endpoint already ran |
| Why is `application/json` not a "simple" request? | HTML forms can't produce it, so it's provably a programmatic call → **preflight** |
| Why can't the attacker send the CSRF token? | The browser auto-sends **cookies**, not tokens — and SOP/CORS stop them reading it |
| What is HSTS's first-request problem? | No policy is known yet, so the first plaintext hop is strippable. Fixed by the **preload list** |

---

## 📌 Still to cover

Topics not yet written up, roughly in order of interview value:

- [ ] Testing security — `@WithMockUser`, `SecurityMockMvcRequestPostProcessors`, `jwt()` post-processor, testing a JWT filter
- [ ] Spring Authorization Server — running your own OAuth2/OIDC provider
- [ ] Keycloak operationally — realms, clients, client scopes, protocol mappers, admin API (the *consuming* side is in [[11. OAuth2 Resource Server]] and [[12. OAuth2 Login and SSO]])
- [ ] Multi-tenancy — tenant claims, per-tenant issuers, row-level data isolation (the model is in [[14. RBAC and ABAC]])
- [ ] Reactive security — WebFlux, `ReactiveSecurityContextHolder`, `SecurityWebFilterChain`
- [ ] Rate limiting and account lockout — `isAccountNonLocked` wired to real counters
- [ ] Secrets management — key rotation, vault integration, where the signing key actually lives
- [ ] mTLS and service-to-service auth in a service mesh
- [ ] XSS in depth — output encoding, sanitising rich text, and how it defeats every other defence ([[16. CSRF Deep Dive]] §5, [[17. HTTP Security Headers]] §2)

---

Source material: `raw_data.txt` in this folder.
